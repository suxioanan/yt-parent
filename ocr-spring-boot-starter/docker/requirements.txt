# ============================================================
# Python 依赖
# ============================================================
# paddlepaddle / paddlepaddle-gpu 已由基础镜像自带，无需重复安装
# CPU 镜像：paddlepaddle/paddle:2.6.1
# GPU 镜像：paddlepaddle/paddle:2.6.1-gpu-cuda11.8-cudnn8.6-trt8.5
# ============================================================

paddleocr==2.9.1
fastapi==0.115.6
uvicorn==0.34.0
python-multipart==0.0.20
Pillow>=10.0.0
PyMuPDF>=1.25.0

# 省份简称补全已内置在 ocr_server.py 中，不再依赖 PaddleX
